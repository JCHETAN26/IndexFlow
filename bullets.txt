% IndexFlow — resume bullets, framed per role (same \resumeProjectHeading + \resumeItem style as bullet-samples.txt)
% Repo: https://github.com/JCHETAN26/IndexFlow
% Honesty notes: all numbers are from LOCAL evaluation on labeled fixtures / a synthetic latency
% benchmark, not production. Strongest framings: AI Engineer and SWE. DE/MLE are supporting.
%   Verified: hybrid MRR 0.98 (vs 0.96 semantic / 0.92 keyword) on a 27-query labeled set;
%   RAG 98% faithfulness / 92% refusal on a 32-question LLM-judged set; ACL leak test 9/9;
%   latency p50 flat 1K->100K chunks (semantic ~2-5ms, keyword ~8-14ms). Pick 2 bullets per role.

---

### IndexFlow — AI Engineer

\resumeProjectHeading{\textbf{IndexFlow} $|$ \textit{TypeScript, RAG, pgvector, Elasticsearch, Ollama (llama3.2/qwen2.5), Next.js} $|$ \href{https://github.com/JCHETAN26/IndexFlow}{\underline{GitHub}}}

\resumeItem{\textbf{Built a permission-aware hybrid retriever} fusing Elasticsearch BM25 and pgvector semantic search through a normalized weighted blend, reaching \textbf{0.98 MRR} (vs 0.96 semantic / 0.92 keyword) on a labeled 27-query evaluation set with the blend weight chosen by an offline sweep.}

\resumeItem{\textbf{Engineered a grounded RAG layer with a local LLM-as-judge hallucination eval} (generator separate from judges), scoring \textbf{98\% faithfulness and 92\% refusal correctness} across a 32-question adversarial set, and proved via a \textbf{9/9 leak test} that restricted documents never enter retrieval or a generated answer.}

\resumeItem{\textbf{Enforced per-document access control on both retrieval legs} using a denormalized Elasticsearch ACL \texttt{terms} filter and an equivalent Postgres SQL predicate behind a shared retriever that requires a viewer, so a restricted chunk cannot be retrieved or cited even when it is the top semantic match.}

---

### IndexFlow — Machine Learning Engineer (ML evaluation)

\resumeProjectHeading{\textbf{IndexFlow} $|$ \textit{Transformers.js (all-MiniLM-L6-v2), pgvector, Ollama, TypeScript} $|$ \href{https://github.com/JCHETAN26/IndexFlow}{\underline{GitHub}}}

\resumeItem{\textbf{Built a deterministic retrieval evaluation harness} measuring recall@k and MRR with an offline weight sweep, running real Elasticsearch BM25 against an ephemeral index and exact pgvector KNN inside a rolled-back transaction so scores are reproducible and never pollute live data.}

\resumeItem{\textbf{Designed a bias-controlled LLM-as-judge evaluation} separating generation (llama3.2) from judging (bespoke-minicheck + qwen2.5), scoring per-claim faithfulness, citation correctness, and refusal on a \textbf{32-question} multi-hop/distractor set that surfaced two real hallucination failures.}

\resumeItem{\textbf{Generated 384-dim embeddings fully in-process} with Transformers.js (mean-pooled, L2-normalized), keeping the retrieval pipeline and its evaluation \textbf{offline and API-key-free}, including in CI.}

---

### IndexFlow — Software Engineer (Full-Stack)

\resumeProjectHeading{\textbf{IndexFlow} $|$ \textit{TypeScript, Next.js 15, React 19, PostgreSQL, Prisma, Auth.js} $|$ \href{https://github.com/JCHETAN26/IndexFlow}{\underline{GitHub}}}

\resumeItem{\textbf{Built a full-stack Next.js 15 (App Router) application} with \textbf{12 API routes}, a token-streaming NDJSON answer endpoint tolerant of client disconnects, a permission-scoped documents UI with self-serve sharing (public / user / group grants), and Google sign-in via Auth.js driving per-user access control.}

\resumeItem{\textbf{Modeled a group-based document ACL across 7 Prisma migrations} and enforced it on both retrieval legs behind a viewer-required shared retriever, verified by a \textbf{9/9 permission leak test} and an 8/8 sharing-lifecycle check that assert visibility flips on grant, revoke, and public toggles.}

\resumeItem{\textbf{Prevented stored-content XSS in search highlighting} by HTML-escaping snippets before swapping Elasticsearch sentinel tokens to \texttt{<mark>} tags, and restricted document mutations to owners with typed, status-mapped error responses.}

---

### IndexFlow — Backend / Data Engineer

\resumeProjectHeading{\textbf{IndexFlow} $|$ \textit{TypeScript, Node.js, BullMQ, Redis, PostgreSQL/pgvector, Elasticsearch} $|$ \href{https://github.com/JCHETAN26/IndexFlow}{\underline{GitHub}}}

\resumeItem{\textbf{Built an asynchronous ingestion pipeline on a BullMQ/Redis worker} (extract $\rightarrow$ chunk $\rightarrow$ embed $\rightarrow$ dual-write Postgres/pgvector + Elasticsearch) with \textbf{retry and exponential backoff} and idempotent re-indexing, keeping the two stores consistent through an app-generated shared chunk id.}

\resumeItem{\textbf{Benchmarked retrieval latency from 1K to 100K chunks} and showed p50 stays flat with scale (semantic \textbf{~2--5 ms}, keyword ~8--14 ms), isolating the Elasticsearch network hop as the hybrid bottleneck and HNSW index-build time as the linear cost of scaling ingestion.}

\resumeItem{\textbf{Provisioned a 4-service local stack with Docker Compose} (Postgres/pgvector, Redis, Elasticsearch, MinIO) and gated retrieval quality in \textbf{GitHub Actions CI} against real Postgres + Elasticsearch service containers on every PR.}

---

### IndexFlow — best overall (drop-in, role-agnostic)

\resumeItem{\textbf{Built IndexFlow, a permission-aware hybrid search engine} (Elasticsearch BM25 + pgvector) with a grounded RAG layer, reaching \textbf{0.98 MRR} on a labeled evaluation set and proving via a \textbf{9/9 leak test} that restricted documents never leak into search results or generated answers.}

\resumeItem{\textbf{Instrumented retrieval quality, hallucination rate, and access control as runnable evaluations} --- recall@k/MRR gated in CI, a \textbf{98\% faithfulness / 92\% refusal} LLM-judged RAG eval, and a 1K--100K-chunk latency benchmark --- turning "it works" into measured, defensible numbers.}
