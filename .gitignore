node_modules/
.next/
dist/
build/
out/

# env
.env
.env.local
.env*.local

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# os
.DS_Store

# prisma
apps/web/prisma/*.db

# misc
.turbo/
coverage/
.gstack/
*.tsbuildinfo

# eval scratch output (canonical results live in apps/web/eval/RESULTS.md)
.evalrun/

# personal job-prep notes — never commit
docs/Project_Indexflow/
# Untracked in d5a3660, but nothing stopped them being re-added. They reached the public
# history once already (still readable at c0a2c5a); this is what keeps it from happening twice.
bullets.txt
bullet-samples.txt
check123.txt

# playwright
apps/web/test-results/
apps/web/playwright-report/
apps/web/blob-report/
